<odoo>
    <data>
       <record id="project_task_gantt_view" model="ir.ui.view">
            <field name="name">project.task.gantt.view</field>
            <field name="model">project.task</field>
            <field name="arch" type="xml">
                <div class="gantt-view-container">
                    <div t-ref="gantt-container" class="gantt_container" style="width: 100%; height: 600px;"></div>
                     <div t-if="state.isLoading">Cargando...</div>
                     <div t-if="state.error" t-esc="state.error" class="error-message"/>
                </div>
            </field>
        </record>
        <record id="project_task_action" model="ir.actions.act_window">
            <field name="name">Tareas</field>
            <field name="res_model">project.task</field>
            <field name="view_mode">tree,kanban,form,gantt_view</field>
        </record>
     </data>
 </odoo>